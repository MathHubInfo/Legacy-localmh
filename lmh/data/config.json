{
	"self::colors": {
		"type": "bool",
		"help": "Use colors in the output. ",
		"default": false
	},

	"env::git": {
		"type": "string",
		"help": "Path to the Git executable. Auto-detected if empty. ",
		"default": ""
	},

	"env::latexmlc": {
		"type": "string",
		"help": "Path to the latexmlc executable. Auto-detected if empty. ",
		"default": ""
	}, 

	"env::pdflatex": {
		"type": "string",
		"help": "Path to the pdflatex executable. Auto-detected if empty. ",
		"default": ""
	},

	"env::perl": {
		"type": "string",
		"help": "Path to the perl executable. Auto-detected if empty. ",
		"default": ""
	},

	"env::java": {
		"type": "string",
		"help": "Path to the java executable. Auto-detected if empty. ",
		"default": ""
	},

	"env::cpanm": {
		"type": "string",
		"help": "Path to the cpanm executable. Auto-detected if empty. ",
		"default": ""
	},

	"env::pager": {
		"type": "string",
		"help": "A full path to a pager to use for long outputs. If empty, no pager is used. ",
		"default": ""
	},

	"install::sources": {
		"type": "string",
		"help": "Url prefixes to clone git repositories from. Seperated by ;s. ",
		"default": "git@gl.mathhub.info:;http://gl.mathhub.info/"
	},
	"install::noglobs": {
		"type": "bool",
		"help": "Disable globs when installing repositories. ",
		"default": false
	},

	"setup::latexml::source": {
		"type": "string",
		"help": "Default Source for latexml. ",
		"default": "https://github.com/KWARC/LaTeXML.git"
	},

	"setup::latexml::branch": {
		"type": "string",
		"help": "Default branch for latexml. Automatically uses the remote HEAD if undefined. ",
		"default": ""
	},

	"setup::latexmls::source": {
		"type": "string",
		"help": "Default Source for latexmls. ",
		"default": "https://github.com/dginev/LaTeXML-Plugin-latexmls"
	},
	"setup::latexmls::branch": {
		"type": "string",
		"help": "Default branch for latexmls. Automatically uses the remote HEAD if undefined. ",
		"default": ""
	},

	"setup::latexmlstomp::source": {
		"type": "string",
		"help": "Default Source for latexmlstomp. ",
		"default": "https://github.com/KWARC/LaTeXML-Plugin-latexmlstomp.git"
	},
	"setup::latexmlstomp::branch": {
		"type": "string",
		"help": "Default branch for latexmlstomp. Automatically uses the remote HEAD if undefined. ",
		"default": ""
	},


	"setup::stex::source": {
		"type": "string",
		"help": "Default Source for sTeX. ",
		"default": "https://github.com/KWARC/sTeX.git"
	},
	"setup::stex::branch": {
		"type": "string",
		"help": "Default branch for sTeX. Automatically uses the remote HEAD if undefined. ",
		"default": ""
	},

	"setup::mmt::source": {
		"type": "string",
		"help": "Default Source for MMT. ",
		"default": "https://github.com/KWARC/MMT.git"
	},
	"setup::mmt::branch": {
		"type": "string",
		"help": "Default branch for MMT. Automatically uses gh-pages if empty. ",
		"default": "gh-pages"
	},

	"setup::cpanm::selfcontained": {
		"type": "bool",
		"help": "Use self-contained local CPANM repositories. ",
		"default": true
	},


	"gl::private_token": {
		"type": "string",
		"help": "Gitlab Private token for gitlab interaction. Leave blank to prompt for username / password. ",
		"default": ""
	},

	"gl::host": {
		"type": "string",
		"help": "Host for gitlab interaction. Should have a final slash. ",
		"default": "http://gl.mathhub.info/"
	},

	"gl::deploy_branch_name": {
		"type": "string",
		"help": "Name for automatically created deploy branches. ",
		"default": "deploy"
	},

	"gl::projects_url": {
		"type": "string",
		"help": "URL to (public) projects url. Used for ls-remote. ",
		"default": "http://gl.mathhub.info/public/"
	},

	"gl::issue_url": {
		"type": "string",
		"help": "URL to issues page. ",
		"default": "https://github.com/KWARC/localmh/issues"
	},

	"mh::issue_url": {
		"type": "string",
		"help": "URL to issues page for each repository. ",
		"default": "https://gl.mathhub.info/$name/issues"
	},



	"init::allow_nonempty": {
		"type": "bool",
		"help": "Allow to run lmh init in non-empty directories. ",
		"default": false
	},


	"gen::default_workers": {
		"type": "int+",
		"help": "Default number of workers to use for generating pdf and omdoc. ",
		"default": 8
	},

	"gen::pdf::timeout": {
		"type": "int+",
		"help": "Timeout when generating PDFs. In seconds. ",
		"default": 120
	},

	"gl::status_remote_enabled": {
		"type": "bool",
		"help": "If set to true, enables lmh status remote checking by default. ",
		"default": false
	},

	"::eastereggs": {
		"type": "bool",
		"help": "Enable some eastereggs. ",
		"default": true,
		"hidden": true
	}
}
